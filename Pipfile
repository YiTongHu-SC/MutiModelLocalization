[[source]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
verify_ssl = true
name = "pip_conf_index_global"

[packages]
pyyaml = ">=6.0.1"
openai = ">=1.0.0"
pandas = ">=1.3.0"
openpyxl = ">=3.0.0"
pytest = ">=6.0.0"
pytest-cov = ">=2.0.0"
pytest-timeout = ">=2.0.0"
black = ">=21.0.0"

[dev-packages]

[requires]
python_version = "3.13"
